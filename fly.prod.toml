app = "samossa-api-prod"
primary_region = "fra"

[build]
  dockerfile = "Dockerfile"

[env]
  APP_ENV = "production"
  NODE_ENV = "production"
  SERVE_CLIENT = "false"
  PORT = "3000"
  DATABASE_FILE = "/data/data.prod.db"

[[mounts]]
  source = "samossa_data_prod"
  destination = "/data"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1
  processes = ["app"]
